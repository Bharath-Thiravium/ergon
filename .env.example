# Copy this file to .env (local) or .env.production (server) and fill in values
# Never commit .env or .env.production to git

DB_HOST=127.0.0.1
DB_NAME=ergon_db
DB_USER=root
DB_PASS=Saran
DB_PORT=3306

APP_ENV=production
APP_DEBUG=false
SESSION_LIFETIME=1440

# Notification Services (optional)
FCM_SERVER_KEY=your_fcm_key_here
SMS_ACCOUNT_SID=your_sms_sid_here

# SAP PostgreSQL Connection
SAP_PG_HOST=72.60.218.167
SAP_PG_PORT=5432
SAP_PG_DB=modernsap
SAP_PG_USER=postgres
SAP_PG_PASS=your_pg_password
