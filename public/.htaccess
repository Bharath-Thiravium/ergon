# ERGON Public Assets - Development Configuration

# Enable rewrite engine
RewriteEngine On

# Set proper MIME types
<IfModule mod_mime.c>
    AddType text/css .css
    AddType application/javascript .js
    AddType image/x-icon .ico
</IfModule>

# Allow all files for development
Order Allow,Deny
Allow from all