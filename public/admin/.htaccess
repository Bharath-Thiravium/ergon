# Basic HTTP Authentication for admin area
# Create .htpasswd file with: htpasswd -c .htpasswd admin
# Or use online generator: https://www.web2generators.com/apache-tools/htpasswd-generator

AuthType Basic
AuthName "Finance Admin Area"
AuthUserFile /path/to/.htpasswd
Require valid-user

# Deny access to sensitive files
<Files "*.log">
    Deny from all
</Files>

<Files ".htpasswd">
    Deny from all
</Files>